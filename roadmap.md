# Roadmap

The Rust + GPUI application is the only supported implementation.

## Next

- richer Markdown editing and keyboard commands;
- account settings UI for publishing targets;
- image upload workflows and more publishing providers;
- signed release bundles for macOS and Windows.

Feature work should stay in the Rust workspace and include focused tests.
